# Shipped Clack image

`console.bin` is Contiki + ClackShell as loaded at `$0200` (v1.0.0).

Downloads and `./relay65 --load software/images/console.bin` use this file.
`make -C software/contiki` rebuilds it from the `contiki` tree and copies it here.
